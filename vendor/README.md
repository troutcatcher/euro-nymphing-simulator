# Vendored libraries

- `three.min.js` — three.js r158 (UMD build), MIT. Pinned and vendored rather than
  loaded from a CDN so the 3D page works offline, from the filesystem, and as a
  single self-contained file. See `THREE-LICENSE`.
